import { ModuleScriptDataParams } from '@divi/module';

/**
 * Module script data component for Mini Cart module.
 *
 * @param {ModuleScriptDataParams} params Module script data parameters.
 *
 * @returns {JSX.Element}
 */
export const ModuleScriptData = ({ attrs, id, selector, storeInstance }: ModuleScriptDataParams): JSX.Element => {
  return <></>;
};
